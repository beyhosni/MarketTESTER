# MVP Validator - Frontend

This directory contains the Angular frontend.

## Setup
To initialize the Angular application (if not already present):

```bash
npm install -g @angular/cli
ng new mvp-validator-frontend --directory . --routing --style scss --skip-git
```

## Running
```bash
npm start
```
